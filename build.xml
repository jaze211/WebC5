<project name="ch05_ex1_email" default="dist" basedir=".">
    <description>Custom build script for ch05_ex1_email</description>

    <!-- Các thư mục -->
    <property name="src.dir" value="src/java"/>
    <property name="conf.dir" value="src/conf"/>
    <property name="web.dir" value="web"/>
    <property name="build.dir" value="build"/>
    <property name="dist.dir" value="dist"/>
    <property name="war.name" value="ch05_ex1_email.war"/>

    <!-- Xóa build cũ -->
    <target name="clean">
        <delete dir="${build.dir}"/>
        <delete dir="${dist.dir}"/>
    </target>

    <!-- Compile Java -->
    <target name="compile">
        <mkdir dir="${build.dir}/classes"/>
        <javac srcdir="${src.dir}" destdir="${build.dir}/classes" includeantruntime="false"/>
    </target>

    <!-- Build WAR -->
    <target name="dist" depends="compile">
        <mkdir dir="${dist.dir}"/>
        <war destfile="${dist.dir}/${war.name}" webxml="${web.dir}/WEB-INF/web.xml">
            <fileset dir="${web.dir}"/>
            <classes dir="${build.dir}/classes"/>
            <metainf dir="${conf.dir}"/>
        </war>
    </target>
</project>
